# WG Companion v1.2.0

wg-web 的配套 WireGuard 客户端：导入 `.conf` 即显示**姓名 / 授权模式（白名单 · 黑名单 · 全代理）/ 授权网段**，一键开关隧道，**服务端配置自动同步**，**无需打开 WireGuard 官方程序**。

> v1.2.0 新增**账号登录**能力（与平台「VPN 账号」打通），并大幅优化交互手感 —— 拖动排序、单隧道切换、连接状态反馈、零闪动渲染、退出自愈等。
> 桌面端与 Android 端版本号统一升至 **1.2.0**。

## 新增功能（相比 v1.1.0）

### 账号登录（Windows / macOS / Android）

- **登录面板**：「导入配置」右侧新增登录按钮，点击展开登录面板（桌面端为华丽弹层，Android 为卡片对话框）。
- **账号登录**：用平台为本人开通的「VPN 账号」登录；登录成功后**自动拉取你的配置并置顶显示**，卡片带「账号配置」标识与鎏金光晕；退出登录即删除该账号的配置。
- **保存密码**：勾选后用**系统级安全存储**加密保存 —— Windows 走 DPAPI、macOS 走 Keychain（Electron `safeStorage`），Android 走 **Android Keystore（AES-256-GCM）**，三端绝不落明文；系统不支持安全存储时会明确提示并禁用。
- **自动登录**：勾选后下次启动应用会用安全保存的密码**自动登录**并刷新配置（静默进行，失败不影响使用）；勾选此项会自动带上「保存密码」。
- **历史用户名下拉**：点击/聚焦用户名框即展开历史登录用户名；输入时按内容筛选，无匹配时下拉自动消失；选择某个用户名时若其密码曾被保存则一并填入；每条右侧的删除按钮会连同已保存的密码一起删除。
- **保存密码的安全保护**：从历史下拉选中一个已保存密码的用户名时密文自动回填，但「显示密码」按钮置灰且禁用；一旦修改该密文段，应用会先清空已保存的整个密文段并恢复显示密码按钮 —— 保存的密码全程不以明文暴露。

### 界面与交互

- **深色 / 浅色双主题**（三端）：标题栏新增 🌙/☀️ 切换按钮，一键在华丽深色与优雅浅色之间切换，设置自动记忆。
- **拖动卡片排序**：长按卡片即悬浮抬起跟随光标，其余卡片顺滑让位；拖动时显示**跟随滑动的虚线落点预览槽**（「放到这里」标签），松手 FLIP 吸入落点、无跳变无卡顿；置顶账号卡片不可调整位置（轻晃提示）。悬停不显示拖动光标，**长按（开始拖动）时才变抓手**。
- **单击选中 / 双击开关 / 点开关 = 开关**：单击卡片即选中（驱动主环展示），**悬停不再选中**；双击卡片或**直接点卡片上的开关**都可开关隧道，避免误触。
- **悬停卡片轻微浮起**（上浮 + 柔光阴影）。
- **置顶账号卡片鎏金光晕**（外发光 + 内描边呼吸动画）。
- **托盘双击打开主窗口**（单击仍是快捷面板，240ms 去抖避免冲突）。
- **登录按钮改为鎏金主按钮**（与「导入配置」同尺寸同质感），登录后显示账号名，悬停整颗按钮交叉淡入为红色可退出。
- **页面提示移到左下角**（toast），进出场动画自左侧滑入/滑出。
- **自绘关闭确认弹窗**（按钮带悬浮/按压/光泽扫过动画），可勾选「记住我的选择」。

### 单隧道模式（三端一致）

- **一次只跑一条隧道**：开启某条隧道时，若已有其他隧道在线，会**先断开当前、再开启新的**（序贯切换）。
- **切换中把其他隧道置灰**：任一隧道连接/断开期间，其他隧道的开关与卡片按钮全部置灰、不可点击，避免并发切换。

### 连接状态反馈

- **连接中样式**：隧道开启/关闭过程中，开关显示「连接中」样式 —— 琥珀色轨道 + 旋转加载环，不再使用闪烁样式。
- **防重复启动**：同一个隧道在连接中 / 断开中时不允许再次触发。
- **启动成功动画**：状态由「连接中」切到「已开启」时，开关旋钮滑到开启位 + 一次性金色光晕。

### 渲染性能

- **增量渲染（不再闪动）**：配置更新/状态刷新时**不再重建卡片 DOM**，仅更新状态类，CSS 过渡动画得以完整播放。
- **托盘快捷面板同样改为增量渲染**，不再整列重建导致卡片的入场动画反复重放。

### 退出行为与自愈

- **退出自动断开**：退出应用时断开所有已连隧道（Windows 上同时卸载 `WireGuardTunnel$` 系统服务），因此下次打开 app 默认全是断开态、不会自动连。
- **最小化到托盘保持连接**：选择「最小化到托盘」时**不断开隧道**，app 驻留托盘、隧道继续在线。只有真正退出才断开。
- **启动自愈残留隧道**：app 启动时自动检测并清理上次未正常退出（崩溃/被强杀/开机自启）残留的隧道服务，恢复系统路由并弹出提醒。**不再依赖内存状态，而是逐条查询真实服务状态后断开，保证干净退出。**

### 服务端与 GitHub

- **服务端配置自动同步**：导入新版 wg-web 下发的 `.conf` 后，按设定间隔自动拉取最新配置并生效（桌面端 + Android）；隧道在线时自动重新下发。可在「设置」中调整间隔（默认 30 秒）。
- **GitHub 更新检查**：启动后查询 Releases 最新版，发现更高版本时顶部横幅提示「前往下载」。

## 修复

- **退出后残留隧道劫持路由**（关键）：退出清理不再依赖内存状态，改为枚举全部 .conf 并查询每个隧道的真实运行状态后逐一断开；app 启动时也执行自愈清理。避免崩溃/强杀/开机自启后路由被劫持（如全代理模式下本地网段不可达）。
- **其他卡片可拖到置顶卡片上方**：拖拽落点增加钳制逻辑，非账号卡片的最小插入位置为所有账号卡片之后。
- **Windows：安装后不创建桌面 / 开始菜单快捷方式**：已修正勾选状态的读取时机与默认值，默认创建两个快捷方式（取消勾选则不创建），静默安装同样创建。
- **客户端「服务器地址」默认值**：不再从已导入配置的 `wg-meta` 兜底（可能带出 `127.0.0.1` 等无效地址），改为只回填上次成功登录过的地址。
- **SQLite 方言建表失败**：`schema.sqlite.sql` 中 `vpn_account.full_proxy` 使用了 SQLite 不支持的列内联 `COMMENT`，已改为 `--` 行注释。
- **管理端「复制」在非 HTTPS 环境下失效**：加入 `textarea + execCommand('copy')` 回退，只在真正写入成功时才提示成功、失败明确报错并自动选中内容供手动复制。
- **下发的配置不再携带回环地址**：`WG_PUBLIC_URL` 未设置且请求来自 `127.0.0.1 / localhost` 时 `wg-meta` 的 `server` 改为留空。
- **服务端与管理端**：账号管理页滑块切换、VPN 账号管理卡片式展示、启停/密码规则、配置与账号一一绑定、AES-256-GCM 加密落库、`POST /api/client/login` 客户端登录接口等。详见 wg-web 仓库。

## 下载

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows | `WG Companion Setup 1.2.0.exe` | 分步安装器（可选安装目录 + 可选桌面/开始菜单快捷方式 + 控制面板卸载） |
| macOS | `WG Companion-1.2.0-mac.zip` | **Intel 芯片（x64）** · 解压得到 `WG Companion.app`（未签名，首次打开需绕过 Gatekeeper） |
| macOS | `WG Companion-1.2.0-arm64-mac.zip` | **Apple 芯片（M 系列）** · 同上 |
| Android | `app-debug.apk` | 侧载安装包（内嵌官方 WireGuard GoBackend）· debug 签名 |

## Windows

1. 运行 `WG Companion Setup 1.2.0.exe`（需**管理员权限**，隧道要注册为系统服务 `WireGuardTunnel$<隧道名>`）。
2. 安装向导：选择安装目录 → 勾选要创建的快捷方式（默认已勾选）→ 完成。
3. 从 wg-web 用户卡片下载 `.conf`，拖入应用或点「导入配置」即可使用。
4. 或直接用平台账号登录：点「登录」→ 填服务器地址 / 用户名 / 密码 → 登录后配置自动置顶显示。

> ⚠️ 安装包**未代码签名**，Windows SmartScreen 会提示「未知发布者」，点「仍要运行」即可。

## macOS

1. 按芯片下载对应 zip：Intel → `WG Companion-1.2.0-mac.zip`；Apple 芯片（M1/M2/M3…）→ `WG Companion-1.2.0-arm64-mac.zip`。
2. 解压得到 `WG Companion.app`，拖入「应用程序」（也可直接双击运行）。

> 💡 想要 `.dmg` 分发？在**任意一台 Mac** 上执行 `sh desktop/mac-make-dmg.sh "WG Companion-1.2.0-mac.zip"` 即可一键转换。
3. 前置条件：`brew install wireguard-tools`（提供 `wg-quick`）。开启/关闭隧道时系统会弹出管理员密码确认框。
4. 账号登录同样可用：点「登录」→ 填服务器地址 / 用户名 / 密码。

> ⚠️ **首次打开需绕过 Gatekeeper**（包体未做 Apple 开发者签名与公证）。
> - 图形界面：在「访达」里**右键**点 `WG Companion.app` → 选「打开」→ 弹窗里再点一次「打开」；
> - 或命令行：`xattr -dr com.apple.quarantine "/Applications/WG Companion.app"`。

## Android

1. 允许「未知来源」安装后，点击 `app-debug.apk` 安装。
2. 首次开启隧道时系统会弹出 VPN 授权；从文件管理器「用 WG Companion 打开」`.conf` 也可直接导入。
3. **账号登录**：「导入配置」右侧「登录」→ 填服务器地址 / 用户名 / 密码 → 登录后该账号的配置**自动拉取并置顶显示**。

> ⚠️ **服务端需同步更新**：`VPN权限管控平台/` 需更新到含 `POST /api/client/login` 等接口的版本；部署时**停服 → 部署 → `node server.js --init`（执行加列迁移）→ 重启**。

## 相关

- 服务端 / 管理端：[wg-web](https://github.com/496793028/wg-web)

## License

本应用自身代码采用 **PolyForm Shield License 1.0.0**（非竞争性源码可用许可）。详见 [LICENSE](./LICENSE)。

WireGuard® 版权归 Jason A. Donenfeld，相关组件遵循其原始许可（GPLv2 / LGPL-2.1）。