# WG Companion v1.2.0

wg-web 的配套 WireGuard 客户端：导入 `.conf` 即显示**姓名 / 授权模式（白名单 · 黑名单 · 全代理）/ 授权网段**，一键开关隧道，**服务端配置自动同步**，**无需打开 WireGuard 官方程序**。

> v1.2.0 新增**账号登录**能力（与平台「VPN 账号」打通），并修复 Windows 安装后不创建快捷方式的问题。
> 桌面端与 Android 端版本号在本次统一升至 **1.2.0**。

## 更新内容（相比 v1.1.0）

### 客户端 · 账号登录（新）

- **登录按钮**：「导入配置」右侧新增**登录**按钮，点击展开登录面板。
- **账号登录**：用平台为本人开通的「VPN 账号」（用户名 = VPN 配置姓名 + 密码）登录；登录成功后**自动拉取你的配置并置顶显示**，卡片带「账号配置」标识。
- **退出登录**：删除该账号自动拉取的配置（面板底部一键退出）。
- **保存密码**：勾选后用系统级安全存储加密保存（Windows DPAPI / macOS Keychain，基于 Electron `safeStorage`），**绝不落明文**；系统不支持安全存储时会明确提示并禁用该项。
- **自动登录**：勾选后下次启动应用会用安全保存的密码**自动登录**并刷新配置（静默进行，失败不影响使用）；勾选此项会自动带上「保存密码」。
- **历史用户名下拉**：点击用户名框即展开历史登录用户名；输入时按内容筛选，**无匹配时下拉自动消失**；选择某个用户名时，若其密码曾被保存则**一并填入**；每条右侧的删除按钮会**连同已保存的密码一起删除**。
- **关闭确认弹窗**：改为应用内自绘的华丽弹窗（两个按钮均带悬浮/按压/光泽扫过动画），可勾选「记住我的选择」。
- **服务端配置自动同步**：导入新版 wg-web 下发的 `.conf` 后，按设定间隔自动拉取最新配置并生效；隧道在线时自动重新下发。可在「设置」中调整间隔（默认 30 秒）、关闭自动更新、允许自签名证书。
- **GitHub 更新检查**：启动后查询 Releases 最新版，发现更高版本时顶部横幅提示「前往下载」（每整点复查）；异常静默跳过。

### 服务端 / 管理端（新）

- **账号管理页滑动切换**：「平台账号管理」⇄「VPN账号管理」（与「用户访问记录 | 管理员操作审计」同款滑块切换）。
- **VPN账号管理**：卡片式展示（竖屏同样为卡片，支持点击进入二级页、长按批量）；二级页可**启用 / 停用**账号、**查看 / 修改密码**（密码默认隐藏，勾选「显示密码」才展示）。
- **启停规则**：未启用账号密码可为空；启用空密码账号时会要求输入密码，**未输入则自动生成**。
- **新增 VPN 账号**：不输入密码时提交后自动生成密码；用户名与 VPN 配置用户名绑定，创建时同步新建一个**未授权**的 VPN 配置用户。
- **VPN 配置页新增用户**：新增可勾选项「**启用 VPN 账号**」+ 密码框（未输入则提交后自动生成）；**未勾选则默认建立未启用的账号**。
- **配置弹窗**：下方新增「客户端登录账号 + 密码」展示（默认隐藏 + 「显示密码」勾选）；该弹窗**不再点击空白处关闭**，点空白处会**轻微抖动**提示。
- **配置与账号一一绑定**：删除 VPN 配置会同时删除该用户的 VPN 账号，反之亦然；删除前会明确警告会一并删除另一方。
- 口令以 **AES-256-GCM 加密落库**（需可回显给管理员转交本人，与 WireGuard 私钥同一套主密钥）。
- **客户端登录接口**：`POST /api/client/login`（用户名 + 口令换取本账号配置）+ `GET /api/vpn/:id/password`（管理员按需取明文）+ `POST /api/vpn/:id/password`（改 / 重置口令）。

### 修复

- **Windows：安装后不创建桌面 / 开始菜单快捷方式** —— 已修正勾选状态的读取时机与默认值：现在**默认创建**两个快捷方式（取消勾选则不创建），静默安装同样会创建。
- **SQLite 方言建表失败**：`schema.sqlite.sql` 中 `vpn_account.full_proxy` 使用了 SQLite 不支持的列内联 `COMMENT`，导致 `DB_DRIVER=sqlite` 时 `initSchema` 直接语法报错。已改为 `--` 行注释。

## 下载

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows | `WG Companion Setup 1.2.0.exe` | 分步安装器（可选安装目录 + 可选桌面/开始菜单快捷方式 + 控制面板卸载） |
| Android | `app-debug.apk` | 侧载安装包（内嵌官方 WireGuard GoBackend）· 已更新至 v1.2.0：界面显示版本号 + 启动检查 GitHub 更新 |

## Windows

1. 运行 `WG Companion Setup 1.2.0.exe`（需**管理员权限**，隧道要注册为系统服务 `WireGuardTunnel$<隧道名>`）。
2. 安装向导：选择安装目录 → 勾选要创建的快捷方式（默认已勾选）→ 完成。
3. 从 wg-web 用户卡片下载 `.conf`，拖入应用或点「导入配置」即可开关隧道。
4. 或直接用平台账号登录：点「登录」→ 填服务器地址 / 用户名 / 密码 → 登录后配置自动置顶显示。

> ⚠️ 安装包**未代码签名**，Windows SmartScreen 会提示「未知发布者」，点「仍要运行」即可。

## Android

1. 允许「未知来源」安装后，点击 `app-debug.apk` 安装。
2. 首次开启隧道时系统会弹出 VPN 授权；从文件管理器「用 WG Companion 打开」`.conf` 也可直接导入。

> 注：本包为 debug 签名，便于分发测试；正式发布建议改为 release 签名包。
> ⚠️ **账号登录 / 服务端自动同步目前仅桌面端（Windows / macOS）提供**，Android 端本次未包含（如需对等需另行改造 Compose 端）。

> ℹ️ **Android 包已在本仓库重新生成并放入 `dist/`**：执行 `node android/build-apk.js`（沙箱内依赖 `.workbuddy/build/` 下的 JDK17 + Gradle 8.7 + SDK；本机则直接用 Android SDK 跑 `gradle assembleDebug`），产物 `android/app/build/outputs/apk/debug/app-debug.apk` 已复制到 `dist/app-debug.apk`。

> ⚠️ **服务端需同步更新**：`VPN权限管控平台/` 需更新到含 `POST /api/client/login`、`GET/POST /api/vpn/:id/password`、`vpn_account` 登录字段（`login_enabled` / `pwd_enc` / `last_login_at` / `last_login_ip`）的版本；部署时**停服 → 部署新 `server.js` → `node server.js --init`（执行加列迁移）→ 重启**。
> 注意：`login_enabled` / `pwd_enc` 与既有 `status` **相互独立** —— `status` 管配置有效性，`login_enabled` 管登录账号启停。

## 相关

- 服务端 / 管理端：[wg-web](https://github.com/496793028/wg-web)
